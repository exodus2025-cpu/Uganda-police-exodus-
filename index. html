<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Exodus Sacco PWA — Full Featured</title>
  <meta name="description" content="Exodus Sacco PWA with multilingual UI, voice, payments, education, insurance and more." />
  <link rel="icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Crect width='100' height='100' fill='%23007ACC'/%3E%3Ctext x='50' y='55' font-size='50' text-anchor='middle' fill='white' font-family='Arial'%3EX%3C/text%3E%3C/svg%3E">
  <style>
    :root {
      --bg: #f6f8fb;
      --card: #fff;
      --accent: #0066cc;
      --muted: #6b7280;
    }
    html, body {
      height: 100%;
      margin: 0;
      font-family: Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(180deg, var(--bg), white);
      color: #0f172a;
      transition: background 0.3s, color 0.3s;
    }
    body.dark {
      --bg: #0b1220;
      --card: #1a1f33;
      --accent: #3399ff;
      --muted: #a0aec0;
      background: var(--bg);
      color: #e6eef8;
    }
    .app {
      max-width: 980px;
      margin: 0 auto;
      padding: 12px;
    }
    header {
      display: flex;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
    }
    header h1 {
      font-size: 20px;
      margin: 0;
    }
    .card {
      background: var(--card);
      border-radius: 12px;
      padding: 14px;
      box-shadow: 0 6px 18px rgba(12,15,20,0.06);
      margin: 12px 0;
      transition: background 0.3s, color 0.3s;
    }
    .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
    }
    @media (max-width: 720px) {
      .grid {
        grid-template-columns: 1fr;
      }
    }
    button {
      background: var(--accent);
      color: white;
      border: 0;
      padding: 10px 12px;
      border-radius: 10px;
      cursor: pointer;
      font-weight: 600;
      transition: background 0.3s;
    }
    button:hover {
      background: #005bb5;
    }
    input, select, textarea {
      width: 100%;
      padding: 8px;
      border-radius: 8px;
      border: 1px solid #e6e9ee;
      font-size: 1rem;
      transition: background 0.3s, color 0.3s;
      background: var(--card);
      color: inherit;
    }
    label {
      display: block;
      font-size: 13px;
      margin-bottom: 6px;
      color: var(--muted);
      font-weight: 600;
    }
    footer {
      font-size: 13px;
      color: var(--muted);
      text-align: center;
      margin-top: 20px;
    }
    #map {
      height: 280px;
      border-radius: 8px;
    }
    .small {
      font-size: 13px;
      color: var(--muted);
    }
    .row {
      display: flex;
      gap: 8px;
      align-items: center;
    }
    .controls {
      display: flex;
      gap: 8px;
      align-items: center;
      flex-wrap: wrap;
    }
    .large-font {
      font-size: 1.25rem;
    }
    /* Text style selectors */
    .font-selector {
      margin-top: 6px;
      display: flex;
      gap: 6px;
      flex-wrap: wrap;
    }
    .font-selector button {
      padding: 6px 10px;
      font-size: 0.9rem;
      background: var(--muted);
      color: var(--card);
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
    }
    .font-selector button.active {
      background: var(--accent);
      color: white;
    }
  </style>
  <link
    rel="stylesheet"
    href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
    crossorigin=""
  />
</head>
<body>
  <div class="app" id="appRoot">
    <header>
      <div
        style="
          width: 56px;
          height: 56px;
          border-radius: 12px;
          background: linear-gradient(135deg, #0066cc, #00aaff);
          display: flex;
          align-items: center;
          justify-content: center;
          color: white;
          font-weight: 700;
          font-size: 22px;
        "
      >
        EX
      </div>
      <div>
        <h1 data-i18n="appTitle">Exodus Sacco — Demo PWA</h1>
        <div class="small" data-i18n="tagline"
          >Multilingual & voice-enabled demo for accessibility</div
        >
      </div>
      <div style="flex: 1"></div>
      <div class="controls">
        <label for="langSelect" class="small">Lang</label>
        <select id="langSelect" aria-label="Language selector">
          <option value="en">English</option>
          <option value="sw">Kiswahili</option>
          <option value="lg">Luganda</option>
          <option value="lu">Luo</option>
        </select>
        <button id="toggleLarge" title="Large text">A+</button>
        <button id="toggleTheme" title="Dark mode">🌓</button>
      </div>
    </header>

    <!-- Authentication Card -->
    <section class="card" id="authCard">
      <h2 data-i18n="signIn">Sign in</h2>
      <div class="small" data-i18n="signinHelp">
        Use IPPS and Force Number. Voice commands are available.
      </div>
      <div style="margin-top: 10px" id="authForms">
        <label data-i18n="ipps">IPPS</label>
        <input
          id="ipps"
          placeholder="e.g. 100001"
          aria-label="IPPS"
          autocomplete="username"
        />
        <label style="margin-top: 8px" data-i18n="forceNo">Force Number</label>
        <input
          id="forceNo"
          placeholder="e.g. F001"
          aria-label="Force Number"
          autocomplete="current-password"
        />
        <div
          style="margin-top: 10px; display: flex; gap: 8px; flex-wrap: wrap"
        >
          <button id="signInBtn" data-i18n="signinBtn">Sign in</button>
          <button
            id="registerBiometricBtn"
            style="background: #0b8f4a"
            data-i18n="enableBiometric"
          >
            Enable Biometric
          </button>
          <button id="voiceHelpBtn" style="background: #444" title="Start listening">
            🔊
          </button>
        </div>
      </div>
      <div
        id="authProfile"
        style="display: none; margin-top: 12px"
        aria-live="polite"
      >
        <div class="row">
          <strong id="userDisplay">User</strong>
          <div style="flex: 1"></div>
          <button
            id="signOutBtn"
            style="background: #bb2d3b"
            data-i18n="signout"
          >
            Sign out
          </button>
        </div>
        <div class="small" id="userMeta">Member since: —</div>
      </div>
    </section>

    <div class="grid">
      <!-- Balance Card -->
      <section class="card" aria-label="Balance Information">
        <h3 data-i18n="balance">Balance</h3>
        <div id="balanceArea">
          <div style="font-size: 28px; font-weight: 700">
            UGX <span id="balanceAmt">0</span>
          </div>
          <div class="small">
            <span data-i18n="lastUpdated">Last updated</span>:
            <span id="balanceUpdated">—</span>
          </div>
          <div style="margin-top: 12px">
            <button id="refreshBalance" data-i18n="refresh">Refresh</button>
          </div>
        </div>
      </section>

      <!-- Withdrawals Card -->
      <section class="card" aria-label="Savings Withdrawals">
        <h3 data-i18n="withdrawal">Savings Withdrawal</h3>
        <label data-i18n="amount">Amount (UGX)</label>
        <input
          id="withdrawAmount"
          type="number"
          min="1000"
          aria-label="Withdrawal amount"
          autocomplete="off"
        />
        <label style="margin-top: 8px" data-i18n="purpose">Purpose</label>
        <input
          id="withdrawPurpose"
          placeholder="e.g. School fees"
          autocomplete="off"
        />
        <div style="margin-top: 8px; display: flex; gap: 8px">
          <button id="withdrawBtn" data-i18n="submitWithdraw">
            Submit Withdrawal
          </button>
          <button id="viewHistoryBtn" style="background: #555" data-i18n="history">
            History
          </button>
        </div>
        <div
          id="withdrawMsg"
          class="small"
          style="margin-top: 8px; color: var(--muted)"
          aria-live="polite"
        ></div>
      </section>
    </div>

    <!-- Share Purchases and Dividends Section -->
    <div class="grid">
      <section class="card" aria-label="Share Purchases">
        <h3 data-i18n="sharePurchase">Share Purchases</h3>
        <label data-i18n="sharesAmount">Number of Shares</label>
        <input
          id="sharesAmount"
          type="number"
          min="1"
          aria-label="Number of shares to purchase"
          autocomplete="off"
        />
        <button id="buySharesBtn" style="margin-top: 10px;">
          Buy Shares
        </button>
        <div
          id="sharesMsg"
          class="small"
          style="margin-top: 8px; color: var(--muted)"
          aria-live="polite"
        ></div>
      </section>

      <section class="card" aria-label="Dividends">
        <h3 data-i18n="dividends">Dividends</h3>
        <div id="dividendsInfo" style="font-size: 1rem; font-weight: 600;">
          UGX <span id="dividendsAmt">0</span>
        </div>
        <button id="refreshDividends" style="margin-top: 10px;">
          Refresh Dividends
        </button>
      </section>
    </div>

    <!-- Bank Account Linking and Payment Gateways -->
    <section class="card" aria-label="Bank and Payment Methods">
      <h3>Bank Account & Payment Methods</h3>
      <label for="bankAccount">Bank Account Number</label>
      <input id="bankAccount" placeholder="Enter your bank account number" autocomplete="off" />
      <label for="bankName">Bank Name</label>
      <input id="bankName" placeholder="Enter your bank name" autocomplete="off" />
      <button id="saveBankBtn" style="margin-top: 10px;">Save Bank Info</button>
      <div
        id="bankMsg"
        class="small"
        style="margin-top: 8px; color: var(--muted)"
        aria-live="polite"
      ></div>
      <hr style="margin: 20px 0" />
      <h4>Payment Gateways</h4>
      <label for="paymentGatewaySelect">Select Payment Gateway</label>
      <select id="paymentGatewaySelect" aria-label="Select payment gateway">
        <option value="">-- Select --</option>
        <option value="Mobile Money">Mobile Money</option>
        <option value="Google Pay">Google Pay</option>
        <option value="PayPal">PayPal</option>
        <option value="Binance">Binance</option>
      </select>
      <button id="testPaymentGatewayBtn" style="margin-top: 10px;">
        Test Payment Gateway
      </button>
      <div
        id="paymentGatewayMsg"
        class="small"
        style="margin-top: 8px; color: var(--muted)"
        aria-live="polite"
      ></div>
    </section>

    <!-- Utility Payments Section -->
    <section class="card" aria-label="Utility Payments">
      <h3>Utility Payments</h3>
      <label for="utilityTypeSelect">Select Utility Type</label>
      <select id="utilityTypeSelect" aria-label="Select utility type">
        <option value="">-- Select --</option>
        <option value="Electricity Bill">Electricity Bill</option>
        <option value="Water Bill">Water Bill</option>
        <option value="Internet Subscription">Internet Subscription</option>
        <option value="TV Subscription">TV Subscription</option>
        <option value="Airtime Purchase">Airtime Purchase</option>
        <option value="School Fees">School Fees</option>
        <option value="Transport Ticket">Transport Ticket</option>
        <option value="Goods Payment">Goods Payment (Local/International)</option>
        <option value="Other">Other</option>
      </select>

      <label for="utilityAccount">Account / Reference Number</label>
      <input
        id="utilityAccount"
        placeholder="Enter account or reference number"
        autocomplete="off"
      />

      <label for="utilityAmount">Amount (UGX)</label>
      <input
        id="utilityAmount"
        type="number"
        min="100"
        placeholder="Enter amount"
        autocomplete="off"
      />

      <label for="utilityNotes">Notes (optional)</label>
      <textarea
        id="utilityNotes"
        rows="2"
        placeholder="Additional info"
      ></textarea>

      <button id="submitUtilityPaymentBtn" style="margin-top: 10px;">
        Submit Payment
      </button>
      <button
        id="viewUtilityHistoryBtn"
        style="margin-top: 10px; background: #555"
      >
        Payment History
      </button>

      <div
        id="utilityPaymentMsg"
        class="small"
        style="margin-top: 8px; color: var(--muted)"
        aria-live="polite"
      ></div>
    </section>

    <!-- Branch Locator -->
    <section class="card" aria-label="Branch Locator">
      <h3 data-i18n="branchLocator">Branch Locator</h3>
      <div class="small" data-i18n="branchHelp">
        Tap a marker to view branch details. Use search to filter by district/city.
      </div>
      <div style="margin-top: 8px; display: flex; gap: 8px">
        <input
          id="branchSearch"
          placeholder="Search city/district"
          aria-label="Search branches"
        />
        <button id="locateMe
        
